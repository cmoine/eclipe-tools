package org.eclipse.etools.ei18n.preferences;

import org.eclipse.core.runtime.preferences.AbstractPreferenceInitializer;

public class EI18NPreferenceInitializer extends AbstractPreferenceInitializer {
	@Override
	public void initializeDefaultPreferences() {
        //        IPreferenceStore store=Activator.getDefault().getPreferenceStore();
        //        for (JavaMappingExtension ext : JavaMappingExtensionManager.getInstance().getAll()) {
        //            try {
        //                store.setDefault(ext.getId(), ext.getJavaMapping().getTemplate());
        //            } catch (IOException e) {
        //                Activator.log(IStatus.ERROR, "Failed to load template", e); //$NON-NLS-1$
        //            }
        //        }
	}
}
